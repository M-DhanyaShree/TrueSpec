# TrueSpec Environment Configuration
# Database Configuration (MySQL local-only)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=truespec

# Backend API Configuration
PORT=5000
NODE_ENV=development

# Frontend Configuration (Vite)
VITE_API_BASE_URL=/api
